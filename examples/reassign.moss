#!/usr/bin/env moss

assume Std {
  fn main() {
    let foo = char::a;
    var bar = foo;
    bar = char::c;
    let baz = bar;
    bar = char::b;
    putchar(foo);
    putchar(char::newline);
    putchar(bar);
    putchar(char::newline);
    putchar(baz);
    putchar(char::newline);
  }
}
