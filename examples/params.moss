#!/usr/bin/env moss

assume Std {
  fn foo(second: Char, first: Char) {
    putchar(first);
    putchar(char::newline);
    putchar(second);
    putchar(char::newline);
  }

  fn main() {
    foo(char::g, char::h);
  }
}
