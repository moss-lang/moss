#!/usr/bin/env moss

assume Std {
  val c: Char;

  assume c {
    fn print_it() {
      putchar(c);
      putchar(char::newline);
    }

    fn wait_for_it() {
      print_it();
    }
  }

  fn main() {
    bind c=char::h;
    wait_for_it();
  }
}
